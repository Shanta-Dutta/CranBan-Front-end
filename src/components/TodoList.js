import React,{useState} from 'react'

function TodoList() {
    const [todos, setTodos] = useState([])

    return (
        <div>
            <hi> What's the plan </hi>
        </div>
    )
}

export default TodoList
